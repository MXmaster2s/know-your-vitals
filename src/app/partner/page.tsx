"use client";

import { UploadReports } from "@/components/upload-reports";

export default function PartnerPage() {
  return (
    <UploadReports
      title="Add your partner's data"
      blurb="Their reports live alongside yours, and each of you sees both. Useful when you eat the same meals and walk the same mornings."
    />
  );
}
